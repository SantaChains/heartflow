# hack.md — 本仓「合理 hack」清单

**定位**：`hack.md` 收录的是**刻意为之、且找不到更好写法**的工程手段——它们看起来像坏味道，
但换成「教科书写法」只会更差。每条都要能回答同一个问题：**为什么不用那个更干净的写法？**

与其它文档的分工：

| 文档 | 回答什么 |
|---|---|
| `list.md` | 做了什么、什么时候做的（实施进度 + 明确不做） |
| `best.dev.md` | 外部项目有什么值得借鉴的（A/B/C 分档） |
| `AGENTS.md` | 面向 agent 的硬约束（改代码前必读） |
| **`hack.md`（本文）** | **哪些「丑写法」是被论证过的，别去"修"它** |

维护纪律：**A / B 两档只收录已在代码里、且能指到具体行号的条目**；每条必须写清「代价/边界」，
不许只写好处。新增 hack 要同时给出**反例**（为什么干净写法不行）。
**C / D 两档性质不同**：它们是**手段图谱 / 理论参考**，多数条目本仓并未采用，故**豁免**"必须已在代码里"，
但每条必须带一列**本仓现状**（已用 / 未用 / 被阻挡 / 不适用），防止把参考当事实。
　· **C 档** = 硬件 / ISA / 内存模型 / UB / 硬件特性层；**D 档** = 纯源码写法（不含硬件/系统配置，C++/Rust 通用）。

---

## 0. 判据：什么算 hack，什么不算

三条判据，按被问到频率排序。

### 0.1 panic 类：`justified` 还是 `debt`

唯一的判据是一句话：**是否存在「同样清晰且局部」的非 panic 写法？**

- **有** → 就是 `debt`，去改（哪怕它当前不可达）。改它买到的是「对将来编辑的耐久性」。
- **没有** → 就是 `justified`，在命中行**行尾**或**紧邻上一行**标注 `// panic-ok: <理由 ≥8 字符>`。

`Regex::new(<字面量>)` 是第一类的原型：Rust 不提供不可失败的 regex 构造函数，
去掉 panic 就得把 `Result` 穿过所有调用点——为一个「字面量写错了」的编译期错误付出运行时的复杂度。
`fn f() -> Result<…>` 里的 `path.parent().expect(…)` 是第二类的原型：`ok_or_else(…)?` 一样长，panic 直接消失。

> **不要**把 `debt` 读成「生产会 panic 的 bug 清单」。`debt` 只表示「存在同样清晰的局部改写」。

### 0.2 锁污染类：结果是「错」还是「安全降级」

`if let Ok(guard) = mutex.lock()` / `.lock().ok()?` 是一类**独立的**缺陷，
**棘轮的 8 个 pattern 一个都抓不到它**（它不是 panic 形态）。判据：

- 污染后结果**变错** → 必须恢复（`unwrap_or_else(PoisonError::into_inner)`）。
- 污染后只是**降级但仍安全** → 保留显式 `Err` 分支，并在注释里写清为什么。

详见 A4。

### 0.3 什么**不算** hack（防止滥用本条清单）

- ❌ **用 `try/catch`（Rust 里 `catch_unwind`）掩盖失败** —— 掩盖不是处理，本仓全仓**没有** `catch_unwind`。
- ❌ **为让门禁变绿而标注 `panic-ok`** —— 标注是「写下理由供 review 攻击」，不是免死金牌。
- ❌ **为让门禁变绿而 `--update` 棘轮基线** —— 那是唯一会**赦免回归**的命令（见 B1/B7）。
- ❌ **加宽 `allow` 压掉 clippy** —— `clippy::all` 零告警是硬门；`pedantic` 已放宽，够用。

---

## A. 源码层

### A1 · `build.rs` 扫盘找 `rc.exe`，用 `RC_PATH` 绕过注册表

**位置**：`crates/cli/build.rs:60` `discover_rc()`、`:27` 设 `RC_PATH`

**问题**：`winresource` 用 `reg query HKLM\SOFTWARE\Microsoft\Windows Kits\Installed Roots`
找 Windows SDK（其 `get_sdk`）。本机 `reg.exe` 被安全中心**程序黑名单**拦截 → SDK 找不到 →
`bin\x64\rc.exe` 解析失败 → cli crate 编不过 → **整个 workspace 的测试门失效，cli 改动退化为盲改**。

**hack**：`build.rs` 自己**不碰注册表**，直接扫
`C:\Program Files (x86)\Windows Kits\10\bin\<ver>\{x64,x86,arm64}\rc.exe`
（按 `CARGO_CFG_TARGET_ARCH` 选架构、新版本优先），把结果写进 **`RC_PATH`** 环境变量。

**为什么这招有效**：读 winresource 源码确认 `compile_with_toolkit_msvc` 的**第一个分支就是 `RC_PATH`**，
优先级最高 —— 设了它，注册表那次探测就失去影响。这是**用对方的最优先入口覆盖它自己的探测**，
比 fork 依赖或改仓库级策略都轻。

**边界**：只填 `RC_PATH` 当调用方**没设**时（显式值优先）；`RC_PATH` 是**构建期**环境变量，
不进入运行时。

### A2 · `HF_REQUIRE_WINRES` 双模开关：本机降级、发版致命

**位置**：`crates/cli/build.rs:23`

**问题**：版本资源缺失在两处含义完全相反 —— 本机开发时它**无害**（只是 exe 版本号显示 0.0.0.0），
发版时它**必须致命**（否则发出的二进制没有版本资源）。

**hack**：默认降级为 `cargo:warning` 并继续；设 `HF_REQUIRE_WINRES=1` 才 panic。
发版 workflow（`.github/workflows/release.yml`）显式设该变量。

**为什么**：`cfg!(debug_assertions)` 之类的「按构建模式分」是不可靠的替代 ——
本机也会跑 `--release`。把「要不要严格」做成**显式环境变量**，
让「谁需要保证」自己声明，比猜上下文清楚。

### A3 · `// panic-ok:` 标注体系（当前 7 处）

**位置**：`scripts/check_panic_budget.py`（标注解析）、`scripts/panic_budget.json`（本机基线）

**问题**：棘轮若要求「panic 数归零」，就会把刻意为之的 panic 一并钉成债，
然后逼维护者**要么写更差的代码、要么频繁 `--update`** —— 而棘轮一旦被频繁刷新就等于没有。

**hack**：分三类 —— `test`（`#[cfg(test)]` 内，永不计数）、`justified`（有标注，可增长但每条都进 diff）、
`debt`（其余，硬门禁只许变小）。标注理由 <8 字符视为未标注（`panic-ok: ok` 是橡皮图章，不是理由）。

**当前 7 处**（全部已核验为「无更好写法」）：

| 位置 | 内容 | 为什么 justified |
|---|---|---|
| `crates/api/src/retry.rs:23` | `.expect("static reqwest configuration must build")` | 该函数**返回 `Client`**；去掉 panic 得把 `Result` 穿过所有调用点 |
| `crates/runtime/src/bash.rs:481` | `child.stdout.take().expect("piped stdout")` | 上面刚 `Stdio::piped()`；改写要为不可能的分支造一条没人能恢复的路径 |
| `crates/runtime/src/bash.rs:483` | 同上（`stderr`） | 同上 |
| `crates/runtime/src/redact.rs:40` | `.expect("valid url-userinfo regex")` | `Regex::new(<字面量>)`，Rust 无不可失败构造函数 |
| `crates/runtime/src/redact.rs:50` | `.expect("valid auth-header regex")` | 同上 |
| `crates/runtime/src/redact.rs:63` | `.expect("valid labeled-secret regex")` | 同上 |
| `crates/runtime/src/redact.rs:74` | `.expect("valid prefixed-key regex")` | 同上 |

**棘轮认的 8 个 pattern**（`panic!` / `todo!` / `unimplemented!` / `unreachable!` /
`.expect(` / `.unwrap(` / `.expect_err(` / `.unwrap_err(`）：

- 后三者是 2026-09-22 补入的**盲区** —— `unreachable!` 就是 panic 宏；
  `expect_err`/`unwrap_err` **在 `Ok` 分支 panic**，与名字给人的直觉正好相反。
- 补入时生产命中数为 **0** → 不动任何现有数字，纯粹为**接住未来第一个**。
- **刻意不含 `assert!`**：生产里的 `assert!` 是「我要强制的不变量」，本仓数百处几乎全在 `#[cfg(test)]`，
  计入只会淹没信号。

**边界**：脚本是**lexer-lite**，不是编译器 —— 注释与字符串内容会被挖空，
`#[cfg(test)]` 靠**大括号计数**跳过。它的失误方向是「把测试行当成债」（抬高基线），
而不是「让债藏起来」，所以可信。

### A4 · `PoisonError::into_inner` 恢复被污染的锁（+ 两个刻意**不**恢复的）

**位置**：`crates/tools/src/todo.rs:84`、`crates/cli/src/theme.rs:214,225`、
`crates/provider/src/cassette.rs:199,326`、`crates/cli/src/storage.rs:100`（`lock_state` 收口）、
`crates/mcp/src/http.rs:144,162`、`crates/runtime/src/{schema.rs:35,47, file_ops.rs:856,1078,1105}`

**问题**：`if let Ok(guard) = mutex.lock()` 与 `.lock().ok()?` 在锁被污染时**静默放弃工作**。
污染只在「持锁时 panic」后发生，所以后果不是崩溃而是**悄悄变错**：

| 站点 | 污染时的旧行为 |
|---|---|
| `runtime/schema.rs` | `.lock().ok()?` → **全进程的工具入参校验整体失效** |
| `cli/storage.rs` `register_secret` / `registered_secrets` | 凭据**退出剔除集** → 该 key 可能**落到盘上** |
| `cli/storage.rs` `rebind_conversation` | `/clear`、resume 后仍写**旧会话** |
| `cli/storage.rs` `current_session_id` | 换新 id → 会话被**拆成两段** |
| `cli/storage.rs` `note_mirror_rewrite` | 不置 `force_full` → 尾巴可能被**追加到陈旧基** |
| `cli/storage.rs` `mirror_to_store` | 静默不写镜像 → 历史库与 JSON **静默漂移** |
| `mcp/http.rs` | 丢 `Mcp-Session-Id` → 后续请求被服务端当**新会话** |
| `runtime/file_ops.rs` ×3 | 并发扫描的命中**静默丢失** |

**hack**：用 `unwrap_or_else(PoisonError::into_inner)` **恢复**那个 guard。
可证性：被守护的值（`SessionState`、`Vec<TodoItem>`、调色板、结果收集器）字段互相独立
（`Option`/`Vec`/`String`/计数），**没有「半写会破坏跨字段不变量」的风险**，所以污染值仍然可用。

**两个刻意保留显式 `Err` 的**（别顺手"统一"掉，它们的降级**更安全**）：

- `crates/cli/src/storage.rs:180` `redact_for_save` —— 污染时退回**永远正确的全量清洗**（只是慢）。
- `crates/cli/src/main.rs:1941` 折叠渲染器 —— 污染时渲染**全部**输出，而不是丢弃。

`crates/cli/src/main.rs:1952` 的 `/expand` 污染时只是「静默失效、不产错结果」，影响低，**未改**。

**反例**：`crates/mcp/src/client.rs:111` 用 `.map_err(|_| McpError::Protocol("transport lock poisoned"))?`
—— 对**不可恢复**的传输锁，把污染升级成类型化错误是**对的**，不该改成 `into_inner`。
判据仍是 0.2：这里污染后**无法安全继续**，不是「安全降级」。

### A5 · `rename_with_transient_retry`：**只重试 `PermissionDenied`**

**位置**：`crates/runtime/src/session.rs:272`、`crates/provider/src/load.rs:597`（同义副本）

**问题**：Windows 上实时杀软会在 `fs::write` 返回后**再持有**那个新写的临时文件几毫秒，
使本该原子的 `rename` 直接失败于 `ACCESS_DENIED` —— 转录写入因为一个**调用方无法处理**的原因而死掉。

**hack**：重试 3 次、间隔 50ms，**但只重试 `PermissionDenied`**；其它 error kind **首次即返回**。

**为什么必须限定错误种类**：`provider/load.rs` 旧版对**所有**错误一律重试 3 次，
于是「目标是个目录」这种真失败也白转两圈、最后还多睡 20ms 才报错。
**限定 kind 之后，"重试"才重新等于"等待一个已知的瞬时窗口"**，而不是"掩盖真失败"。

**边界**：两个 crate 无公共依赖，故是**同义副本**而非共享实现；注释已互相指认，
改一处必须改另一处。

### A6 · 编译 jsonschema 时**不持锁**

**位置**：`crates/runtime/src/schema.rs` `compiled_validator`

**问题**：校验器缓存在 `Mutex<HashMap<String, Validator>>` 里（每个 schema 只编一次，
因为编译是这里最贵的一步）。但旧写法**持锁编译** —— 一个慢 schema 会挡住**其它线程的缓存查询**。

**hack**：先在锁内查缓存 → **放锁** → 编译 → 回锁插入。
两线程同时撞上同一个未缓存 schema 只会各编一次：**结果确定、插入幂等**，重复编译无害。

**为什么不是「先持有再优化」**：这是典型的「用一点可能的重复计算，换掉一把长持有的锁」，
且重复的代价上界是常数（每个 schema 至多多编一次）。

### A7 · cassette 用**装饰器**，不加 enum 变体

**位置**：`crates/provider/src/cassette.rs`、`crates/provider/src/lib.rs`

**问题**：录制/回放要走**真的**会话循环，最省事的写法是给 `TransportClient` 加一个 enum 变体
（`TransportClient::{Live, Replay}`）。

**hack**：`CassetteClient<C>` 泛型装饰器，**不改**已有 enum。

**为什么**：该 crate 是 `publish = true` 的**公开 API**，加 enum 变体是**破坏性变更**，
会让已发布的 `heartflow-provider` 用户编译失败。装饰器对已有调用方零影响。
**保真点选在 `AgentEvent`（解析后）而非 wire 层** —— 于是回放顺带成为「提示装配回归夹具」。

### A8 · Windows 进程树：`CREATE_NO_WINDOW` + `taskkill /T`（不是 Job Object）

**位置**：`crates/runtime/src/bash.rs:395,402`、`:419` `kill_process_tree`

**问题**：真正的「围栏」是 Win32 Job Object（`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`，
子进程随容器一起死）。但它需要 `CreateJobObjectW` 等 `unsafe` FFI。

**hack**：`unsafe_code = "forbid"`（**是 `forbid` 而非 `deny`，局部 `#[allow]` 无法覆盖**）
→ 走安全路径：`CREATE_NO_WINDOW`（std 的 `creation_flags`，安全 API）+ 超时瞬间 `taskkill /F /T` 全树清扫。

**代价（诚实标注，写在 `kill_process_tree` 注释里）**：二者性质不同 ——
Job Object 是**围栏**（子进程在容器内，随容器一起死）；`taskkill /T` 是**时点清扫**
（在超时那一刻枚举后代并终止）。后者对守规矩的子孙足够，但理论上存在
**「清扫枚举后新生的进程逃逸」**的窄窗口。

**非 Windows 平台保留原状**：该平台子进程刻意与终端同进程组以便 `Ctrl+C` 可达，
重新分组会**静默破坏中断投递** —— 这个取舍也写在注释里。

### A9 · `ignore` 并行 walker 收尾的 `into_inner().unwrap_or_default()`

**位置**：`crates/runtime/src/file_ops.rs:863,1084,1114`

**问题**：并行扫描（`ignore` 的 walker，`:833` `build_search_walker(...).run(...)`，
**不是 rayon** —— 本仓无 `rayon` 依赖）把结果推进一个局部 `Mutex<Vec<_>>`，收尾时要取回 `Vec`。

**hack**：`results.into_inner().unwrap_or_default()` —— 污染时退化为**空结果**。

**为什么这里可以**：scanner 内部**已经**用 `PoisonError::into_inner` 逐条推入（A4，`:855-857`），
所以 `into_inner` 在此只是「取出」而非「决定是否放弃」；退化为空是**兜底**而非主路径。

### A10 · `simd-json` 序列化 MB 级转录

**位置**：`crates/runtime/src/session.rs:161`

**问题**：转录每回合**整体重写**一次，且可达 MB 级。

**hack**：`simd_json::serde::to_string_pretty`。SIMD 的字符串分类在这一档数据上**明显压过** `serde_json`。

### A11 · `Image` block 按固定 1500 token 估算

**位置**：`crates/runtime/src/compact.rs:6` `IMAGE_TOKEN_ESTIMATE = 1_500`、`:491` 使用处

**问题**：真值取决于解码后的像素，需要**先解码**才能算准 —— 而估 token 是**发给模型之前**的事。

**hack**：固定 1500。

**为什么**：这是一处**已知不精确**的妥协，代价是「估算偏差」，收益是「不需要解码就能在窗口预算里
给图片留位」。要精度就得把解码拉进预算路径 —— 那是拿一个必然的开销换一个可能的精度。

### A12 · `unix_secs` / `unix_millis` 的 `unwrap_or_default`

**位置**：`crates/cli/src/storage.rs:29,36`

**问题**：`SystemTime::now().duration_since(UNIX_EPOCH)` 在系统时钟被设到 1970 之前时会 `Err`。

**hack**：`.unwrap_or_default()`（退化为 0）＋ `i64::try_from(...).unwrap_or(i64::MAX)`（防溢出）。

**为什么**：这是**时间戳**，退化值只是「排序时排到最前」，不影响正确性；
为它把 `Result` 穿过整条持久化路径是**用真复杂度换一个不可能的场景**。

---

## B. 构建 / 环境层（本机）

> 这一节不是「本仓的设计」，而是**这台机器上与工具链搏斗得出的结论**。
> 换机器请重新验证，别当普适真理。

### B1 · `panic_budget.json` 是本机基线、**刻意不入库**

**位置**：`scripts/check_panic_budget.py`（`load_or_adopt_baseline`）、`.gitignore:33`

它约束的是「**这台机器上**不许比上次更差」，不是源码树的属性 → 跑不进 CI，新克隆也没有基线。
脚本把两种「没有可用基线」**分开**对待：

- **缺失**（新机器 / 首次 / 刚删）= 正常 → **采纳当前树为新起点**、打印明细、exit 0。
- **损坏**（无法解析 / 结构不对）= 真错误 → exit 1 **拒绝猜测**（静默覆盖只会把数据丢失藏起来）。

**反例（旧行为）**：缺基线就 exit 1 并提示「请 `--update`」—— 这等于**教人无脑 `--update`**，
而 `--update` 恰恰是唯一会**赦免回归**的命令，棘轮当场失效。现在 `--update` 会逐条打印
「这次赦免了哪些回归」。

与 `scripts/bench-gate.sh` 的约定**同向**（都是机器本地量测、从不提交），别再当成一对相反项。

### B2 · `ci.yml.bak` 是**刻意停用**的质量门

GitHub 不识别 `.bak`。Actions **不跑代码检查** → 质量门全靠本地手动跑。
改这个文件名之前先问「谁来跑质量门」。

### B3 · `reg.exe` 与 `wmic.exe` 被安全中心**程序黑名单**拦截

任何依赖注册表探测或 WMI 的工具都会因此失败（winresource 找 SDK 即一例，见 A1）。
部分 cargo 调用会在 stderr 打印该提示，**无害**。不要试图绕过（"Do NOT retry"）。

### B4 · 盘满时链接器报的是 `LNK1108` / `LNK1318`，**不是代码或 PDB 问题**

D: 盘 100% 占满时（实测仅剩 2.2MB / 一度为 0），报错是
`LNK1108 无法在 0x… 处写入文件` 与 `LNK1318 PDB 错误: LIMIT` —— 看着像代码，其实是**磁盘写不进去**。
最快的释放手段是删 `target/debug/incremental`（纯缓存，可重建）。

### B5 · cargo 偶发 `os error 5 拒绝访问` 是**瞬时文件锁**，重试即过

出现在写 `target/debug/.fingerprint/*/invoked.timestamp` 或 GC incremental 目录时，
是 Windows/杀软的瞬时现象。**别把它误诊成权限或配置问题去改 build 配置。**

### B6 · 这四个诊断工具在本机**不可信**，换工具

| 工具 | 症状 | 替代 |
|---|---|---|
| `du` | PATH 上是 Sysinternals 版，弹 EULA、输出 UTF-16 横幅 | Python `os.walk` 累加 |
| `find -iname` | **漏掉确实存在的文件** | Glob 工具 / Python |
| PowerShell 工具 | **取不到 stdout**（`Write-Output "x"` 都是空） | 二进制探测或 Bash |
| Bash 的 `grep` | **漏掉确实存在的匹配**（exit 1） | Grep 工具 / Python |

### B7 · 同一文件的多处编辑**不要**放在同一条消息里

会「**报成功、实际没落**」：2026-09-22 对 `storage.rs` 一次发 6 个 Edit，
落 4 个、1 个明确报 `EBUSY`、**1 个报成功却完全没改**。
铁律：**同文件批量改完必须 grep 复核**，别信返回值。

---

## C. 高性能 hack 手段图谱（理论参考，**多数本仓未采用**）

> **性质声明**：A / B 两档是**本仓已经在用、且被论证过**的东西；本节不同 —— 它是
> **手段图谱 / 备查字典**，绝大多数条目**本仓并没有采用**。每一条都带一列「本仓现状」，
> 防止把参考当事实。本节**不构成"待办"**：图谱里的手段要进本仓，必须先过 C.0 的四条守则。
>
> **定义（本节的 hack 口径）**：**不是**常规算法调参，而是**利用硬件 / ISA / 内存模型、
> 底层未定义行为、硬件特性的非常规手段**。共性风险：**维护成本高、平台绑定强、必须实测**，
> **不能无脑上**。

### C.0 判据：有利 / 有害、取舍、效应边界

三条**正交**判据，缺一不可：

1. **有利 vs 有害** —— 判据不是"快 vs 慢"，而是**「可测量的净收益是否覆盖它带来的维护与可移植代价」**。
2. **取舍（tradeoff）** —— 每个 hack 都必须在同一句话里说清**换来什么、又换掉了什么**。
   说不清"换掉了什么"的，多半是坏的 hack。
3. **效应边界（effect boundary）** —— 在**什么输入规模 / 平台 / 编译器版本 / CPU 代际**下成立；
   出边界即**失效甚至倒退**。边界不写明 = 下一个人会在错误的规模上依赖它。

**四条守则（把 C 档手段纳入本仓的前提）**：

- **先测量，后 hack**。没有 `bench` 支撑的"优化"是猜测 —— 本仓有 `scripts/bench-gate.sh` 的量测约定（B1）。
- **hack 必须可局部回退**：一个 feature 开关、或一个函数边界，能一行关掉。
- **效应边界必须写进注释**，和 A 档条目同级要求。
- **平台绑定显式标注**：`x86_64` / `MSVC` / 特定 `rustc` / CPU 特性，不许假装可移植。

**「有利 / 有害」的快速对照**（拿不定时先查这张）：

| 信号 | 倾向**有利** | 倾向**有害** |
|---|---|---|
| 有基准 | 有 before/after 数字与误差棒 | "理论上更快" |
| 局部性 | 影响面锁在一个函数/模块 | 渗透进公共 API 或数据模型 |
| 可回退 | 一个 flag 可关 | 关掉要改多处 |
| 可移植 | 有 `cfg` 分派或安全降级路径 | 假定单一平台/ISA |
| 可观测 | 有断言/测试钉住行为 | 只能"跑起来看着对" |
| 依赖面 | 用成熟的库（simd-json 等） | 自造 unsafe 轮子 |

> **定级口径**：**工程可用** = 主流工具链/库即支持、有安全降级、可进 review；
> **学术极端** = 需 nightly / `unsafe` / 特定硬件、只适合研究或数据面极致场景。

---

### C.1 内存布局 Hack

> 目标：让数据在**缓存行 / 对齐 / 访存模式**上更快，而非"算法更优"。效应全部集中在
> **memory-bound** 的循环上；对 I/O-bound 的代码基本无效。

| 手段 | 定级 | 适用边界（何时有利） | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **字段重排降 padding** | 工程可用 | 结构体被**大量分配/遍历**、且字段类型大小不齐 | 收益随字段组合波动，可能为 0；`#[repr]` 之外的顺序是编译器自由的 | 未用（未测） |
| **`#[repr(packed)]`** | 学术极端 | 需**逐字节**精确布局/协议解析 | 破坏对齐 → 取字段可能生成多次非对齐访问，**在部分架构上是 UB 或陷阱**；需 `unsafe` 读 | **被阻挡**：`unsafe_code = "forbid"`（Cargo.toml:55） |
| **`#[repr(align(N))]` / 缓存行填充** | 工程可用 | 高竞争原子量**跨线程写**（防伪共享） | 每条 padding 浪费内存；单线程场景纯亏 | 未用（无高竞争热点实测） |
| **AOS → SOA** | 工程可用 | 循环只碰**少数列**、向量化友好 | 随机访问/整体搬运时更差；改动渗透全数据结构 | 未用 |
| **Arena / bump / slab 分配** | 工程可用 | 生命周期一致、大量小对象、分配成瓶颈 | 释放只能整体回收；长生命周期会涨内存 | 未用（走 `mimalloc`，见 C.7） |
| **软件预取 `_mm_prefetch`** | 学术极端 | 可预测的**大跨度顺序/链式**访问 | 需 intrinsics+`unsafe`；预测错会污染缓存，**常常反效果** | **被阻挡** |
| **冷热字段分离（hot/cold split）** | 工程可用 | 热路径只碰小部分字段、结构体巨大 | 多一次间接跳转；改动渗透类型定义 | 未用 |
| **位压缩 / niche 优化** | 工程可用 | 内存是硬约束、字段值域窄 | 拆装带来 CPU 开销；Rust 的 `Option<&T>` niche 已自动省 | 未用（未测） |
| **SmallVec / SSO 短字符串** | 工程可用 | 绝大多数实例很小、少数溢出 | 溢出即退化到堆+搬家；类型替换污染面广 | 未用 |
| **mmap 映射文件代替 `read`** | 工程可用 | 大文件**随机/部分**读、重复访问 | 顺序一次读更慢；SIGBUS 边界坑 | 未用 |

**要点**：这一整节的收益**只在内存受限的热循环上兑现**。本仓是**会话/IO 驱动**的 CLI，
主耗时在**网络往返与文件写**（A10 的瞬态重试就是证据），不在紧凑循环 —— 所以整节对
本仓**预期收益低**，这也是它们尚未被采用的原因，而不是"没想到"。

---

### C.2 CPU 指令级 / SIMD Hack

> 目标：让**同一条热点循环**用更少周期。效应边界极其依赖**数据规模与分支可预测性**。

| 手段 | 定级 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **手写 SIMD intrinsics（`core::arch`）** | 学术极端 | 稳定热点、数据可批、标量版已证瓶颈 | 需 `unsafe`；**架构绑定**（x86 vs ARM 两套）；可读性骤降 | **被阻挡** |
| **依赖自动向量化** | 工程可用 | 循环体简单、无别名、分支少 | 编译器常拒绝向量化；需读优化报告 | 已用（间接，见下 simd-json） |
| **分支消除（branchless / cmov / 掩码）** | 工程可用 | 分支**高度不可预测**且代价高 | 分支可预测时**更慢**；需实测 | 未用 |
| **查表 / jump table** | 工程可用 | `match` 分支多且分布均匀 | 间接跳转**预测失败**代价可能高于链式比较；边界检查 | 未特意用 |
| **`const fn` / 编译期查表** | 工程可用 | 输入在编译期已知 | 仅限可 const 求值的场景 | 未检查 |
| **`target-cpu=native` / `target-feature`** | 工程可用 | 只为本机跑、发布另编 | **发布物绑死 CPU**；跨机可能非法指令 | 未用（无 `.cargo/config.toml`） |
| **`likely`/`unlikely`（Rust 用 `#[cold]`/布局近似）** | 工程可用 | 冷路径几乎不走 | 标错会**反向拖慢**；Rust 无 GCC 同款内建，较粗 | 未用（无 `#[cold]`） |
| **循环展开 / 不变量外提** | 工程可用 | 短循环、固定次数 | 代码膨胀、指令缓存压力 | 交给编译器 |
| **SWAR 位技巧** | 学术极端 | 字节流上的批量判定、且无法用库 | 难读难维护；边界处理易错 | 未用 |
| **`popcnt`/`lzcnt`/`tzcnt` 等硬件指令** | 工程可用 | 位运算本身是热点 | 需 `target-feature`/intrinsics；否则走软实现 | 未用 |
| **FMA 乘加融合** | 工程可用 | 浮点密集、精度可接受 | 改变**浮点舍入**结果，可能破坏位级可复现 | 未用 |
| **非临时存储 / streaming store** | 学术极端 | 一次性写大块、不复用 | 需 intrinsics；用错伤缓存 | **被阻挡** |

**本仓的现实**：heartflow 的"SIMD 收益"**已经通过库兑现** —— `simd-json`（A10，
`runtime/session.rs:161`）把 SIMD 用在了**最值钱的那一处**（MB 级转录序列化），
而**没有**自己写 intrinsics。这是**工程可用路线的正确形态**：用成熟库拿 SIMD 红利，
把 `unsafe` 与架构绑定的成本留给上游。

---

### C.3 编译器层面 Hack

> 目标：**不改代码语义**，改"编译器怎么编"。本档**代价最低、最安全**，是首要杠杆。

| 手段 | 定级 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **LTO（`thin`/`fat`）** | 工程可用 | 多 crate、有跨 crate 内联/去虚化空间 | 构建**变慢**；`fat` 内存占用高 | **已用**：`[profile.release] lto="fat"`（Cargo.toml:35） |
| **`codegen-units = 1`** | 工程可用 | 追求最优化 | 构建**显著变慢**；增量编译失效 | **已用**（Cargo.toml:38） |
| **`panic = "abort"`** | 工程可用 | 不要栈展开、追体积 | **抹掉 panic 回溯**；与依赖行为交互 | **刻意不用**（Cargo.toml:42-46，为保住回溯） |
| **`strip`** | 工程可用 | 发布物 | 去掉调试符号后**难定位**崩溃 | **已用**：`strip="debuginfo"`（保符号表） |
| **PGO（`-Cprofile-use`）** | 工程可用 | 有**代表性**负载可跑训练集 | 训练集不代表性会**误导**；构建链复杂 | 未用（无代表性负载采集） |
| **BOLT（二进制重排）** | 学术极端 | 极致、可接受额外工具链 | LLVM 工具链耦合、易碎 | 未用 |
| **`#[inline]`/`#[inline(always)]`/`#[cold]`/`#[inline(never)]`** | 工程可用 | 实测出的热点/冷点 | 手标常**劣于**编译器判断；污染代码 | 未用（0 处） |
| **单态化替代 trait object** | 工程可用 | 热路径上的动态分派 | 编译膨胀；`dyn` 是**为动态性**才用的 | 部分（依设计，非优化） |
| **const generics / 编译期分派** | 工程可用 | 参数在编译期固定 | 复杂度上升 | 未用 |
| **`build-std` / nightly `-Z`** | 学术极端 | 需裁剪 std、专用目标 | **绑 nightly**、构建不可复现 | 未用（toolchain 钉 1.93.1 stable） |
| **更快链接器（mold/lld）** | 工程可用 | 只影响**构建时长**，不影响产物 | Windows/MSVC 受限；装工具链 | 未用 |
| **编译期求值 / `const` 表** | 工程可用 | 表在编译期可定 | 编译变慢、错误信息难读 | 未检查 |

**要点**：本档**物美价廉**，本仓已经把最大的一块（`release` profile）吃下了（2026-09-22，
见 A/附）。剩下的 PGO 是**下一个候选**，但前提是**先有代表性负载**——否则训练集偏差
会让"优化"变成"为某一条路径提速、其余变慢"。**没有负载就别上 PGO。**

---

### C.4 并发 / 内存模型 Hack

> 目标：在**多核**上少同步、少等待。代价几乎全在**正确性**：错的内存序 = 数据竞争 = UB。

| 手段 | 定级 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **`Ordering::Relaxed` 计数** | 工程可用 | 纯统计/发号，**不承担同步语义** | 需自证"不依赖它做 happens-before" | **已用**：`mcp/client.rs:113` `next_id`、`cli/theme.rs:226` `THEME_GEN`、`file_ops.rs` 计数、`tui.rs:1890` 停机标志 |
| **`Ordering::SeqCst` 同步** | 工程可用 | 需要全序保证的地方 | 全屏障，**最慢**但最省心 | **已用**：`conversation.rs:2215-2218` probe、`retry.rs` 测试计数 |
| **`Acquire`/`Release` 精调** | 学术极端 | 实测 SeqCst 是瓶颈 | 错配即**微妙的竞态**；难测难复现 | 未用（尚未到瓶颈） |
| **无锁数据结构（lock-free / wait-free）** | 学术极端 | 极高竞争、延迟敏感、可接受复杂度 | ABA / 内存回收难题；`unsafe` 为主 | 未用（用 `Mutex`，够用） |
| **伪共享 padding** | 工程可用 | 验证过的高竞争跨线程字段 | 见 C.1；单线程纯亏 | 未用 |
| **批量系统调用（`io_uring`/batching）** | 学术极端 | 海量小 I/O、Linux 专用 | 平台绑定；Windows 无 | 未适用（Windows 主平台） |
| **工作窃取（work-stealing）** | 工程可用 | 不规则并行任务 | 调度开销；小任务反而慢 | **已用（间接）**：`ignore` 并行 walker |
| **自旋锁 vs futex** | 学术极端 | 临界区极短、竞争可控 | 空转烧 CPU；错判毁性能 | 未用（std `Mutex`） |
| **RCU** | 学术极端 | 读多写极少、Linux 内核场景 | 回收期复杂，Rust 生态不成熟 | 未用 |
| **线程本地存储（TLS）** | 工程可用 | 每线程独立状态、避免锁 | 生命周期/析构坑；线程多则开销 | 部分（std 内部） |

**要点**：本仓原子量的用法**健康且克制** —— `Relaxed` 只给**发号/计数**，
`SeqCst` 只给**真同步**（probe、测试），**没有**为了"更快"而把同步降级到 `Acquire/Release`
的赌注。**这是正确的默认**：在没有 profiler 指出"原子操作是瓶颈"之前，
把 `SeqCst` 换成弱序只是**用竞态风险换一点点速度**，属 C.0 表里"倾向有害"那一列。

---

### C.5 GPU Hack

| 手段 | 定级 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| UBO 对齐（std140/std430） | 学术极端 | GPU 渲染/计算管线 | **不适用** | 不适用 |
| Bindless / 描述符索引 | 学术极端 | 海量纹理/资源绑定 | 不适用 | 不适用 |
| Early-Z / 深度预通道 | 学术极端 | 光栅化管线 | 不适用 | 不适用 |
| Compute shader 通用计算 | 学术极端 | 大规模数据并行 | 不适用 | 不适用 |
| 异步计算 / 多流 | 学术极端 | 计算与传输重叠 | 不适用 | 不适用 |
| 合并访存 / 共享内存分块 | 学术极端 | 显存带宽受限 | 不适用 | 不适用 |
| Warp/Wave 级原语 | 学术极端 | 线程组内协作 | 不适用 | 不适用 |

**结论**：**heartflow 是纯 CPU 的 CLI/服务工具，无渲染管线、无数据并行计算内核 →
整个 C.5 不适用。** 保留此节只为图谱完整性；**不要**因为"别人有"就往本仓引入 GPU 依赖。

---

### C.6 数据 / 算法层面的非常规 Hack

> 这一档介于"算法"与"系统"之间：**不改硬件假设，改数据组织与计算策略**。
> 最常被误当成"常规调参"，其实**取舍很硬**。

| 手段 | 定级 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **缓存友好遍历顺序** | 工程可用 | 大表多列、只碰部分列 | 与外部接口/序列化顺序冲突 | 未特意用 |
| **分块 / 平铺（blocking/tiling）** | 工程可用 | 矩阵类、局部性可造 | 边界处理复杂度 | 未用 |
| **非加密 hash（FxHash/AHash）** | 工程可用 | 内部 `HashMap`、无 DoS 面 | 对敌对输入**易碰撞**（哈希洪泛） | 未换（用默认 SipHash） |
| **空间换时间 / 预索引** | 工程可用 | 查询远多于更新 | 内存与**一致性**维护成本 | 部分（缓存类） |
| **增量 / 差分更新** | 工程可用 | 大对象小改动 | 全量重写反而更简单可靠 | 未用（转录走整写，见 A10） |
| **惰性求值 / 记忆化** | 工程可用 | 重复计算、可缓存 | 缓存失效与内存增长 | 部分（validator 缓存，A6） |
| **近似算法（HLL/采样）** | 工程可用 | 只需量级估计、可接受误差 | **精确性换速度**，需场景允许 | 未用 |
| **批量 / burst 合并** | 工程可用 | 高频小事件 | 延迟上升；flush 时机难调 | 部分 |
| **快/慢路径分离（specialize）** | 工程可用 | 常见输入可特化 | 两份代码漂移 | 未特意用 |
| **零拷贝（`Cow`/borrow/`mmap`）** | 工程可用 | 大负载只读/少改 | 生命周期复杂化；`mmap` 见 C.1 | 部分（借用为主） |
| **字符串 interning / 池化** | 工程可用 | 大量重复串 | 全局池的回收与并发 | 未用 |

**要点**：本仓在这一档**最可能拿到真收益**（因为它是 IO/数据形态问题，不是 CPU 问题）——
例如"零拷贝""增量更新""快慢路径"都直接对应它的真实工作负载。但每一项都要**先有 profile**
指明瓶颈，别按"列表好看"去上。

---

### C.7 与本仓的交集：现状快照 + 采纳规则

**现状快照**（2026-09-23 复核）：

| 类别 | 本仓已用 | 本仓被硬约束挡住 | 本仓未用但可考虑 |
|---|---|---|---|
| 内存布局 | — | `repr(packed)`、预取（`unsafe_code="forbid"`） | 字段重排、SOA（**收益低**） |
| CPU/SIMD | `simd-json`（库路线） | 手写 intrinsics | 自动向量化友好改写（先测） |
| 编译器 | `lto="fat"`、`cgu=1`、`strip` | `-Z`/`build-std` | **PGO**（需先有负载） |
| 并发/内存模型 | `Relaxed` 计数 + `SeqCst` 同步、`ignore` 并行 walker | `Acquire/Release` 精调 | 无（当前未到瓶颈） |
| GPU | 不适用 | 不适用 | 不适用 |
| 数据/算法 | validator 缓存、部分零拷贝 | — | 非加密 hash、增量更新（先测） |

**全局约束一句话**：`unsafe_code = "forbid"`（Cargo.toml:55）**一票否决**了本图谱里
所有需要 `unsafe` 的**学术极端**手段（`repr(packed)`、intrinsics、Job Object FFI、无锁结构、
非临时存储）。要让它们进来，**先改这条 workspace 策略**，而不是在某个文件里 `#[allow]`
（`forbid` 连局部 `#[allow]` 都不认）—— 见 A8 的同款论证。

**采纳一条 C 档手段的检查清单**（缺一不采纳）：

1. **有基准**证明它是瓶颈（而非"看起来像瓶颈"）。
2. 在同一句里写清**换来什么 / 换掉什么**（C.0 判据 2）。
3. 写下**效应边界**（规模/平台/编译器/CPU 代际）。
4. **可一行回退**（feature 开关或函数边界）。
5. **本仓现状**一栏从"未用"改成"已用"，并在本条注释里回指 `hack.md` 的对应行。

---

### C.8 具体写法补充（二版稿的落地例证）

> **定位**：C.1–C.6 的表格给的是**判据级**信息（定级 / 边界 / 代价 / 本仓现状）；本节把二版稿里
> **"具体长什么样"** —— 记法、库名、典型场景、更锋利的代价句 —— 补齐，作为 C.1–C.6 的**例证附录**。
> **二版稿的第六节「数据算法」同样在标题处被截断**，其内容已由 **C.6 表格**覆盖（缓存友好遍历 /
> 分块 / 非加密 hash / 增量更新 / 近似算法 / 零拷贝 / interning…），此处不再重复。

**C.1 内存布局**（收益最高、最常用）
- **padding 压缩**：手动重排成员消除隐式填充；**热数据前置、冷数据拆到独立结构体**。
- **`#[repr(packed)]`（Rust）/ `__attribute__((packed))`（C/C++）**：取消对齐。**代价 = 未对齐访问可能触发 CPU 异常、跨平台崩溃**，只在明确场景用。
- **AOS → SOA**：`[Vec3, Vec3, Vec3]` → `{ x:[], y:[], z:[] }`。适合**批处理 / 物理 / 粒子 / ECS**；CPU 预取器连续读**同类型数组**，**SIMD 自动向量化更容易触发**。**魂系 ECS、Bevy 大量使用**。
- **Arena / 线性分配器**：**一次性预分配大块**，对象从 arena 内取、**不调系统堆** → 免碎片 + 免锁；**释放 = 整块销毁，不逐个析构**。适合**短期帧数据 / 临时计算缓冲**。
- **Cacheline 隔离伪共享**：多线程读写的变量落在**同一 64B cacheline** → 乒乓失效；**手动填充**分隔。**Rust 现成物：`std::sync::CachePadded`**。
- **预取 `_mm_prefetch`**：主动把将访存拉进 L1/L2；适合**流式遍历大数组**；**滥用会挤占缓存带宽、反而降速**。

**C.2 CPU 指令与 SIMD**
- **显式 intrinsic**：直接调 **AVX2 / AVX512 / NEON**，绕过编译器自动向量化的保守判断。**风险 = 架构绑定，须做运行时 CPU 特性检测，无检测会直接崩溃**。
- **分支消除（最重要的性能 hack）**：`res = cond ? a : b` → 用 **bitmask 计算、不跳转**；亦可用 `min`/`max`/选择指令。**适合怪物 AI 判定、碰撞检测**。
- **常量传播 / 编译期求值**：**`consteval`（C++）/ `const fn`（Rust）**，运行时直接拿结果。**极端 = 模板元编程**。
- **跳转表**：大量状态判定 → **函数指针存入数组、按索引查表跳转**，避免链式分支预测失效。**适合 FSM 怪物 AI**。
- **`__builtin_expect` / 静态分支提示**：标大概率路径、优化指令排布。**现代 CPU 预测器很强、收益变小，但密集循环仍有用**。

**C.3 编译器**
- **LTO**：跨文件内联、消跨模块边界、降调用开销。**代价 = 编译时间暴涨 + debug 难度急剧上升**。
- **`noinline` / `always_inline` / `cold`**：干预内联策略；**把低频代码标 `cold` 丢进冷代码段，减少 I-cache 占用**。
- **栈分配代替堆分配**：小对象放栈上，栈访问几乎零开销；**栈大小有限，过大对象栈溢出**。

**C.4 并发与内存模型**
- **relaxed 原子无锁**：`Ordering::Relaxed` **仅保证原子读写、不做内存屏障**，可替代 `Mutex`。**风险极高 —— 内存重排会产生逻辑 bug，必须理解弱内存模型**。
- **任务分块 + 工作窃取**：细粒度任务、线程池窃取空闲任务，减少等待。**Bevy / 多数游戏 ECS 内置**。
- **批量提交减少 syscall**：**缓冲攒满一次性 `write`**；高频 syscall 是巨大瓶颈，适合**日志 / 文件 IO**。
  （与 C.4 表的 `io_uring` 区分：**缓冲批量 = 工程可用；`io_uring` = 平台绑定的学术极端**。）

**C.5 GPU**
- **UBO/SSBO 对齐**：严格匹配 GPU 布局（**16B 对齐**），避免驱动隐式拷贝。
- **bindless**：一次性上传资源数组、按索引访问，**减少 drawcall**。
- **Z 预填充 / Early-Z**：先渲深度，**提前丢弃被遮挡像素**，不执行片元着色。
- **计算 shader 批量压缩**：GPU 侧清洗 / 降采样，**减少 CPU↔GPU 传输（PCIe 带宽是瓶颈）**。
- **本仓提醒**：二版稿此节写"适配你 4D 高斯场景" —— 那是**图形 / 引擎模板的口径**；**heartflow 是纯 CPU 的 CLI/Agent 工具、无渲染管线，整节不适用**（与 C.5 表结论一致）。

**C.6 数据算法** —— 见 C.6 表格（二版稿此节被截断，已由表格覆盖）。

---

## D. 代码层性能写法图谱（纯源码 · 通用 vs 偏 hack · C++/Rust 通用）

> **性质声明（同 C 档）**：本节是**理论参考 / 备查字典**，多数条目本仓并未采用；每条带一列
> **本仓现状**，防止把参考当事实。本节**不构成"待办"**。
>
> **与 C 档的分界**：**C 档** = 硬件 / ISA / 内存模型 / 底层 UB / 硬件特性的**非常规手段**
> （`repr(packed)`、intrinsics、内存序、GPU…）；**D 档** = **纯源码写法**，**不含任何硬件 /
> 系统配置**，**C++ / Rust 通用**，面向**游戏 / ECS / AI 数据处理**的常见热点。
> 两档在边界上互指不重复（内联、展开、冷路径、原子序在两边都出现 → D 档只讲"怎么写"，C 档讲"底下的代价"）。

### D.0 判据：通用 vs 偏 hack

**「通用 vs 偏 hack」的唯一判据**：**去掉它，会不会让代码更难懂？**

- **通用写法**：几乎不牺牲可读性，任何正经工程都该做（`with_capacity`、循环外提、预分配）。
- **偏 hack 写法**：要拿**可读性 / 可维护性 / 正确性**去换（手动展开、`unreachable_unchecked`、
  fast-math、类型标签手搓 vtable）。**必须实测**，且**必须有回退开关**。

三条硬边界沿用 C.0：**有基准** / **可一行回退** / **写清效应边界**。

**一条本仓专属提醒**：本仓是 **Rust**。所以下面 **C++ 专属条目一律标「不适用」**，
只在其后给出 Rust 对应物（或直接写"无对应物"）。Rust 没有"隐式拷贝构造 / 异常 / fast-math /
`restrict`"这些东西，照搬 C++ 经验会走偏。

**场景重点**（用户口径：游戏 / ECS / AI 数据处理 —— 本仓**都不做**，此处仅供横向参考）：

| 场景 | 最值钱的档 | 说明 |
|---|---|---|
| 实时游戏 | D.2 循环、D.5 内存布局、D.7 临时对象 | 帧预算硬、缓存敏感、忌堆分配 |
| ECS | D.1 去动态派发、D.4 连续容器 | 组件+系统天然 SOA、热循环按列访问 |
| AI 数据处理 | D.3 窄类型、D.2 分块、D.4 预分配 | 带宽/内存受限、批处理、点云/张量 |

---

### D.1 减少函数调用开销

| 手段（C++ ｜ Rust） | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| 小热点函数内联：`inline` ｜ `#[inline]` | 通用 | 小函数、跨翻译单元/跨 crate 调用 | 内联只是**建议**；**超大函数强制 `always_inline` 会膨胀 I-cache，反而更慢** | 未用（0 处 `#[inline]`；靠 `lto="fat"` 做跨 crate 内联，见 C.3） |
| 消除虚函数 / 动态派发：模板、CRTP ｜ 泛型 / trait 静态分发 | 通用 | 类型集**编译期已知**且调用在热点 | 编译膨胀、代码量爆炸；**运行期才定的多态不能去** | 部分：`dyn` 用在 permission prompter / transport / questioner —— **确需动态性，非优化失误** |
| ECS 用组件 + 系统替代继承 | 通用 | 游戏 / ECS 数据布局 | 抽象重构成本 | 不适用（无 ECS） |
| 手动 vtable / 类型标签 + `match` 替代虚函数 | **偏 hack** | 需近似虚调用但想控布局 | 易错、丢编译器检查、维护面大 | 未用 |
| 减少跨模块调用：LTO + 同翻译单元 + 冷路径抽离标 `cold` | 通用 | 跨 crate 热点 | 见 C.3（构建变慢） | LTO 已用（C.3） |
| 传参优化：大对象传引用/指针、小值按值 | 通用 | 大 `struct` 按值传会整块拷贝 | **反向坑**：Rust `&T` 引入别名/借用限制，部分场景**按值更快**（小 `Copy` 类型） | 未审 |

---

### D.2 控制循环写法（热点循环收益最大）

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **循环外提（loop-invariant hoisting）**：不变的计算/寻址/查表提到循环外 | 通用 | 循环体内**真不变**的东西（含 `let m = mat.matrix` 这类只是绑定的） | 提到外面会**延长借用的生命周期**，可能过不了借用检查 | 交给编译器 / 未审 |
| **循环合并（loop fusion）**：多次遍历同一份数据合成一次 | 通用 | 多次遍历、且中间无依赖 | 合并后寄存器压力上升，可能反优化 | 未用 |
| **循环分块（tiling / blocking）**：切到与缓存匹配的小块 | 通用 | 超大数组、矩阵/粒子/点云 | 边界处理复杂；块大小要调 | 未用（见 C.6） |
| **循环展开（unroll）**：手动 / 编译器指令 | 通用（自动）/ **偏 hack**（手动） | 短循环、固定次数、减少计数器判断 | **过度展开爆 I-cache**；现代编译器通常自己会做 | 交给编译器 |
| **避免循环内分支**：位运算 / 条件选择指令替代 `if` | 通用→需实测 | 分支**高度不可预测**且代价高 | 分支可预测时**更慢**；可读性下降 | 未特意用 |

> `let m = mat.matrix;` 那个例子在 Rust 里要小心：如果 `mat.matrix` 是 `Copy` 的小值，
> 提出来是纯赚；如果是**引用**，提出来等于**延长借用**，很可能直接被借用检查拒绝 —— 这正是 D.0
> "Rust 不能照搬 C++"的典型。

---

### D.3 类型与数值写法

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **选用更窄数值类型**：`u8` 取代 `u32`、`f16` 取代 `f32` | 通用 | **内存带宽受限**（点云、量化、动画位移） | **反向坑**：窄类型算术常需扩展/窄化与掩码指令，**CPU 侧可能更慢**；只有带宽是瓶颈时才赚 | 未特意 |
| **避免隐式类型转换**：循环内反复 `i32↔f32` | 通用 | 手写 C++ 常见 | —— | **不适用**：Rust **无隐式数值转换**，必须显式 `as`/`into` |
| **减少求逆 / `sqrt` / 三角函数**：预计算 LUT 或近似函数 | 通用→**偏 hack**（近似） | 同一函数反复求值、精度可放宽 | 近似会**改变数值结果**；LUT 有内存与误差 | 未用 |
| **避免 NaN / Inf**：`-ffast-math` 类选项 | **偏 hack** | 浮点密集、且**确认不需要 IEEE 语义** | 放宽浮点标准 → 结果不可复现、可能引入错误 | **不适用**：Rust 稳定版**无 `fast-math`**；NaN/Inf 是语义的一部分，不能靠开关丢掉 |

---

### D.4 容器与迭代写法

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **预分配容量**：`Vec::with_capacity` / `.reserve()` / `String::with_capacity` | 通用 | 已知或可估的最终规模，循环内反复 push | 估大了浪费内存 | **已用**：`tools/web.rs:486,573,686,779`（4 处 `String::with_capacity`） |
| **优先连续内存容器**：`Vec`/数组；避免热点遍历 `LinkedList`/树 | 通用 | 热循环、顺序访问 | 树/链表的适用场景（频繁中间插入）被牺牲 | 惯例（`Vec` 为主） |
| **迭代器**：避免链式嵌套 / 多次遍历；连续数组用索引 | 通用（**C++ 味**） | C++ 里链式 `iterator` 可能有虚/间接开销 | **Rust 反例**：迭代器链常被**单态化 + 内联**优化掉，"避免链式"在本仓多属**言之过早** | 未特意（按可读性写） |
| **复用对象 / 缓冲区**：循环内别 `new` / `Box::new`，改更新字段 | 通用 | 循环内高频分配 | 需手工管理状态，易出错 | 未审（`Box::new` 仅在非热路径：错误装箱、`set_hook`、walker 闭包） |

---

### D.5 内存访问与别名控制

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| C++ `restrict` / `__restrict` 声明指针无别名 | 通用 | 手写 C++ 热点循环 | —— | **不适用**：Rust 借用检查天然限制别名，**无 `/` 不需要** |
| Rust：热点减少**多重可变引用** | 通用 | 循环内多个 `&mut` 交织 | 重构成本 | 通用惯例 |
| **只读数据放常量段**：`static` / `const` | 通用 | 大只读表（LUT、字典） | 大 `static` 增加二进制体积 | 部分 |
| **批量读 / 批量写**，避免读写混排 | 通用 | 流式处理大块数据 | 需要缓冲，增加内存 | 未审 |

---

### D.6 错误与分支处理

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **高频循环不用异常**：返回状态码 / bitmask | 通用 | C++ 热循环 | —— | **不适用（异常）**：Rust 无异常；但有对应物 —— **热点里避免 `panic`**，用 `Result`（见 §0.1 / A3） |
| **错误/冷分支标冷路径**：`__builtin_expect` / `likely-unlikely` ｜ `#[cold]` / `core::hint::unlikely` | 通用→**偏 hack** | 冷路径几乎不走 | **标错会反向拖慢**；Rust 的 `unlikely` 长期是 nightly | **未用**：0 处 `#[cold]` / `core::hint` |
| **早返回前置（fail fast first）**：快速失败判断放函数开头 | 通用 | 参数校验、前置条件 | 无（纯赚可读性） | 未审（Rust 常见 guard 风格） |

---

### D.7 消除临时对象

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| C++ RVO / NRVO、`std::move` 移动语义 | 通用 | 返回大对象、容器转运 | —— | **不适用**：Rust **无移动构造**，move 即位拷贝，值语义**天然无隐式深拷贝** |
| **减少隐式 `.clone()`**：热点循环禁止无脑 clone（**Rust 的对应物**） | 通用 | 热循环、大对象 | 换 `&`/`Cow` 可能复杂化生命周期 | 未审：`clone()` 多在**非热路径**（配置、短字符串）、测试里较多；未见热点无脑 clone |
| **表达式合并，减少中间临时** | 通用 | 链式构造大对象 | 可读性下降 | 未审 |

> C++ 的"消除临时对象"**大半在 Rust 里不存在**：RVO/NRVO/移动语义都已被**所有权 + 位拷贝**
> 从语言层面消解。Rust 里唯一对应的是**减少不必要的 `.clone()`**（以及用 `Cow`/借用替代）。

---

### D.8 原子与锁代码写法

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| **缩小临界区**：锁只包必须同步的最小块 | 通用 | 临界区里有可搬出去的计算/IO | 无（纯赚） | 有 `Mutex`；A6 已示范"**放锁编译**"（典型缩临界区） |
| **最弱可行内存序**，不要无脑 `SeqCst` | 通用（**但有前提**） | 实测 `SeqCst` 是瓶颈、且**能自证** happens-before | **反向坑**：没测到瓶颈就降级 = 用竞态风险换速度（见 C.4） | 已按此用法：`Relaxed` 计数 / `SeqCst` 真同步，**且刻意不降级** |
| **避免循环内加解锁** | 通用 | 循环体反复 `lock`/`unlock` | 无（纯赚） | 未审 |

---

### D.9 编译器提示类代码标记

> 用户原文此节被截断（止于 `readon…`），以下为**补齐**的标准集合。

| 手段 | 档位 | 适用边界 | 失效 / 代价 | 本仓现状 |
|---|---|---|---|---|
| C++ `__attribute__((noalias/nounwind/readonly/nonnull/pure/const))`、`restrict` | **偏 hack** | 越过编译器做承诺 | **标错即 UB**；维护者难察觉 | **不适用**：Rust 无等价稳定属性（`noalias` 由 `&`/`&mut` **自动推导**） |
| `#[inline]` / `#[inline(always)]` / `#[inline(never)]` / `#[cold]` | 通用→**偏 hack** | 实测热点/冷点 | 手标常**劣于**编译器判断 | 未用（0 处） |
| `core::hint::unreachable_unchecked` / `assert_unchecked` / `cold_path` | **学术极端** | 可证不可达 | **`unreachable_unchecked` 错用 = UB** | **被阻挡**：是 `unsafe`（`unsafe_code="forbid"`） |
| `std::hint::black_box` | 通用（**仅基准**） | 防止优化掉被测代码 | **不是运行期提速手段**，写进生产是误用 | 未用（无 bench） |
| `#[repr(...)]` / `#[non_exhaustive]` | 布局 / API 语义 | 见 C.1 | 非热点手段 | 见 C.1 |

**要点**：这一档是**最容易误用**的一档 —— 编译器提示是"**给编译器打分**"，
而现代编译器在**没有 PGO**时对分支概率的默认推断往往比人手标得更准。**没基准，别标。**

---

### D.10 与本仓的交集：现状快照 + 采纳清单

**现状快照**（2026-09-22 复核）：

| 类别 | 本仓已用 | 本仓不适用（Rust / 无对应场景） | 本仓未用但可考虑 |
|---|---|---|---|
| D.1 函数调用 | `dyn` 仅用于**确需动态性**处 | 手动 vtable（无 ECS） | 传参按引用/按值、减少 `dyn` 面 |
| D.2 循环 | 交给编译器 | —— | 循环外提/合并（先测） |
| D.3 类型数值 | —— | 隐式转换、fast-math、异常 | 窄类型（仅带宽受限时） |
| D.4 容器迭代 | `String::with_capacity` ×4;`Vec` 为主 | —— | 复用缓冲区（先测） |
| D.5 内存别名 | Rust 借用检查天然覆盖 | `restrict` | 只读表放 `static` |
| D.6 错误分支 | `Result` 惯例（棘轮守 panic） | C++ 异常 | `#[cold]`/`unlikely`（先测） |
| D.7 临时对象 | —— | RVO / 移动语义 | 减少 `.clone()`（先审） |
| D.8 原子锁 | 缩临界区（A6）、内存序克制 | —— | 循环外加解锁（先审） |
| D.9 编译器提示 | 依赖编译器自动判断 | `noalias` 等属性 | —— |

**采纳一条 D 档手段的检查清单**（与 C.7 同，缺一不采纳）：

1. **有基准**证明它是瓶颈。
2. 一句话写清**换来什么 / 换掉什么**。
3. 写下**效应边界**（数据规模 / 编译器 / 场景）。
4. **可一行回退**。
5. 若是 **Rust 里没有对应物的 C++ 手段** → **直接跳过**，别硬造。
6. **本仓现状**一栏从"未用"改成"已用"，并回指本条。

---

## 附：2026-09-22 审计的处置索引

| 动作 | 条目 |
|---|---|
| 棘轮补 3 个 pattern | A3 |
| 11 处锁污染改为恢复、3 处保留 | A4 |
| `provider/load.rs` 与 `session.rs` 语义对齐 | A5 |
| jsonschema 改为放锁编译 | A6 |
| 新增 `[profile.release]`（`lto="fat"` + `cgu=1` + `strip="debuginfo"`，`panic` 保持 `unwind`） | `Cargo.toml`（性能档位，非 hack，但同批） |
| 新增 C 档「高性能 hack 手段图谱」（理论参考，标注本仓现状）+ 二版例证附录 | C.1–C.8 |
| 新增 D 档「代码层性能写法图谱」（纯源码 · 通用 vs 偏 hack · C++/Rust 通用） | D.1–D.10 |

验证门：`fmt --check` 0 / `clippy --workspace --all-targets -D warnings -A pedantic` 0 /
`cargo test --workspace` 561 passed 0 failed / `check_panic_budget.py` `debt=0 justified=7` /
`cargo build --release --bin hf` 0（产物 16.65 MiB，`--version` 与 `doctor` 冒烟 exit 0）。
