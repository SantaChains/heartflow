# 安全政策

## 支持版本

安全修复只针对最新发布版本。发布通道见 README 的安装章节，升级到最新版后再报告。

## 报告方式

不要在公开 Issue 中描述漏洞。请使用 GitHub 的私密漏洞报告：

1. 仓库 Security 页签 → Report a vulnerability（私密漏洞报告）
2. 或通过 GitHub 联系维护者

若未收到确认，48 小时后可在私密渠道追问一次。

## 响应承诺

- 48 小时内确认收到
- 7 天内给出初步评估（影响范围、严重级别、修复计划）
- 修复发布后在 Release 说明中致谢报告者（除非你要求匿名）

## 范围

heartflow 是会执行 shell 命令、持有模型 API 密钥、主动发起网络请求的终端 AI agent。以下属于重点关注面：

- `web_fetch` / `web_search` 的 SSRF 防护绕过（内网、回环、云元数据地址的可达性）
- bash 与文件工具的权限模型逃逸（read-only / workspace-write 门禁的绕过）
- 密钥、会话内容在日志、错误信息、历史库中的意外泄漏
- MCP 握手与远程服务器的信任边界问题
- apply_patch 的校验旁路与任意路径写入

不属于范围：需要物理访问或社会工程的攻击、依赖明显被入侵的下游依赖（请直接报给上游）、对模型本身生成内容的泛化担忧。

## 披露原则

协调披露。修复发布前请不要公开细节；发布后我们会在 Release 说明与 CHANGELOG 中注明安全修复项。
